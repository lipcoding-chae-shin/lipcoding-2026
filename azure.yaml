# yaml-language-server: $schema=https://raw.githubusercontent.com/Azure/azure-dev/main/schemas/v1.0/azure.yaml.json
name: lipcoding-2026
metadata:
  template: lipcoding-2026@1.0.0
services:
  web:
    project: .
    language: js
    host: containerapp
    docker:
      path: ./Dockerfile
